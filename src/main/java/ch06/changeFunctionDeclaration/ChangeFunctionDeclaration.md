- 좋은 이름을 떠올리는데 함수의 목적을 설명해보면 좋다.
- 매개변수는 함수를 사용하는 문맥을 설정한다. 
- 전화번호 포맷팅에 사람을 넘기는 대신 전화번호만을 넘기면 다른 모듈(사람)과의 결합을 제거할 수 있다. 그런데 다른 함수에서 넘기는 게 객체로 넘기게 되어 그 객체의 속성을 여러 개를 사용할 경우에는 내부 로직은 복잡해지지만 함수의 캡슐화 수준이 높아지는 것이다.
- 정답은 없다.
